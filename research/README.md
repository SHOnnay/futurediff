# Research branches

`original-design-branch/` is the cleaned user-provided implementation/research branch. It has its own `go.mod` and is intentionally excluded from the authoritative root module. It is retained for controlled ports of Slack, PostgreSQL, benchmarks, resource-normalization, and other experiments.
