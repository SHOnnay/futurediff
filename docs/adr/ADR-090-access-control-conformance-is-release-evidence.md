# ADR-090 — Access-control conformance is release evidence

Every release must validate default denial, agent-safety classification, capability scope/expiry, and durable single use. This remains local evidence rather than external IAM certification.
