# ADR-083 — Resource amplification is policy bounded

**Decision:** Transaction, effect, execution, patch and verification cardinalities are bounded by a versioned local policy before the next resource is allocated.
