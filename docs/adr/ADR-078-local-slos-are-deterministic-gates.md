# ADR-078: Local SLOs are deterministic gates

FutureDiff SLO evaluation uses ledger metrics, semantic audit results, daemon reachability, and maintenance state. Thresholds are versioned JSON and cannot change ledger state.
