# ADR-077: Effect dependencies have portable graphs

Transaction, repository, verification, approval, and external-effect dependencies can be exported as JSON, Mermaid, or Graphviz DOT. The graph is a read-only projection and contains no provider payload bodies or credentials.
