# ADR-092 — Sharing never delegates administration

Transaction grants are limited to `read` and `operate`. Approval, commit, recovery, abort, and access administration remain owner/all-scope/capability authority.
