# ADR-094 — Access changes have an independent hash chain

Ownership, grant, and revocation evidence is kept in a payload-minimized global SHA-256 chain and covered by semantic audit and signed checkpoints.
