# ADR-089 — Authorization simulation has no runtime authority

Policy explanation is an offline deterministic computation. It cannot consume capabilities, write audit evidence, or call transaction handlers.
